const backendUrl = 'https://rtc-uat-api.reality.org.in';

const API_URLS = {
  AUTH: {
    SIGN_IN_USERS: `${backendUrl}/api/v1/system/sign-in`,
    CREATE_USER: `${backendUrl}`,
    DELETE_USER: `${backendUrl}`,
    UPDATE_USER: `${backendUrl}`,
  },
};

export default API_URLS;
